# # scheduler.py

# from apscheduler.schedulers.background import BackgroundScheduler
# from .access import check_and_save_to_slack  # 실제 함수가 위치한 경로로 변경해야 합니다.

# def start():
#     scheduler = BackgroundScheduler()
#     scheduler.add_job(check_and_save_to_slack, 'interval', minutes=1)
#     scheduler.start()
